{
  "name": "ecoo-frontend",
  "private": true,
  "scripts": {
    "build": "cd landing && npm run build && cd ../admin && npm run build && cd ../company && npm run build && cd ../citizen && npm run build",
    "dev": "cd landing && npm run dev"
  }
}
